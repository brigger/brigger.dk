.DS_Store
.claude/

# Photo pipeline artifacts — generated locally by scripts/optimize-photos.sh
# from originals in photos/Yannick/. Not committed: originals are huge (~1.8 GB)
# and private; the two resized directories are derived output; manifest.json is
# regenerated by the script. The VPS receives them via rsync in deploy.sh.
photos/Yannick/
photos/Yannick-web/
photos/Yannick-thumb/
photos/manifest.json
